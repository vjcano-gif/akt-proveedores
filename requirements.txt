# Versiones verificadas en Python 3.14 (la que usa Streamlit Community Cloud).
# Se usan mínimos en vez de fijaciones exactas para que el resolutor tome
# siempre una rueda precompilada y no intente compilar desde fuente.
streamlit>=1.64.0
SQLAlchemy>=2.0.54,<2.1
pandas>=3.0.3
plotly>=7.1.0
openpyxl>=3.1.5
bcrypt>=5.0.0
psycopg2-binary>=2.9.11
requests>=2.32.0\npymupdf>=1.28.2\nrapidocr>=3.9.2\nonnxruntime>=1.30.0\n